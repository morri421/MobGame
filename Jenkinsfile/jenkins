pipeline {
   agent any

   stages {
      stage('Checkout') {
         steps {
             git 'https://github.com/morri421/MobGame.git'
         }
      }
      stage('Build') {
         steps {
             sh "mvn clean package"
         }
      }
   }
}